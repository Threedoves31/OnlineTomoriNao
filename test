/* 消息 */
.message {
    display: flex;
    align-items: flex-start; /* 垂直对齐顶部 */
    margin-bottom: 15px;
}

/* 消息框 */
.message-content {
    flex-grow: 1;
    display: flex; /* 让内容和时间戳在同一行 */
    align-items: flex-end; /* 时间戳与消息底部对齐 */
}

/* 消息文本 */
.message-text {
    background: #fff;
    padding: 8px 12px;
    border-radius: 4px;
    max-width: calc(100% - 60px); /* 防止时间戳被挤到下一行 */
    word-wrap: break-word; /* 自动换行 */
}

/* 右对齐消息样式 */
.message.right-aligned {
    flex-direction: row-reverse; /* 反转布局顺序 */
}

.message.right-aligned .avatar {
    margin-right: 0;
    margin-left: 10px;
}

.message.right-aligned .message-text {
    background: #95ec69; /* 微信绿色气泡 */
    margin-right: 0;
    margin-left: 10px;
}

.message.right-aligned .timestamp {
    margin-left: 0;
    margin-right: 10px;
}